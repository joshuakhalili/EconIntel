import { usePageTitle } from '@/components/chrome/AppShell';
import { EmptyBlock } from '@/components/Page';

// Stub. Built once the chart components land — see the migration order.
export default function ExplorePage() {
  usePageTitle('ExplorePage');
  return <EmptyBlock>Not ported yet.</EmptyBlock>;
}
