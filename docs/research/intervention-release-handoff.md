# Diffusion intervention — release handoff

The current, evidence-backed handoff is [release-current-status.md](release-current-status.md).

That record supersedes the September10checkpoint, including its old counts and deployment order. In particular, deploy the null-safe application before applying the qualitative report-value seeds; an application-only rollback afterward is unsafe. Git history retains the earlier checkpoint.
